// This module is intentionally left minimal in production to avoid exposing API keys in the browser.
// Use secure server-side proxy via `secureApiService` instead of initializing OpenAI here.

export default null;